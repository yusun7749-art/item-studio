name = "haven-ai-execution-engine"
main = "src/index.js"
compatibility_date = "2026-07-01"

[vars]
AI_PROVIDER = "openai"
OPENAI_MODEL = "gpt-5-mini"
ALLOWED_ORIGIN = "https://yusun7749-art.github.io"

# Secrets must be configured in Cloudflare and must never be committed:
# wrangler secret put OPENAI_API_KEY
# wrangler secret put HAVEN_ACCESS_TOKEN
